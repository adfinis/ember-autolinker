
import { autolink } from 'dummy/helpers/autolink';
import { module, test } from 'qunit';

module('Unit | Helper | autolink');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = autolink([42]);
  assert.ok(result);
});

